export default {
  add: (a, b) => a + b,
  sub: (a, b) => a - b,
  mul: (a, b) => a * b,
  div: (a, b) => a / b,
  pow: (a, b) => a ** b,
  unaryNegation: a => -1 * a,
  unaryPositive: a => 1 * a,
};
